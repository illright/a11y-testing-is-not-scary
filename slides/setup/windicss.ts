import { defineWindiSetup } from '@slidev/types'

export default defineWindiSetup(() => ({
  theme: {
    extend: {
      fontFamily: {
        heading: '"Raleway", sans-serif',
      },
    },
  },
}))
